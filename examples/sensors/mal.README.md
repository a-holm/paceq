# mal.sh: the blank form

Eight lines, the starting point. It reads the cursor and answers "nothing
new". Copy it and fill in the part between the cursor are the two markers that
says what is new since the cursor. A skip never moves the cursor, so the same
question is asked again.

