# Create the paceq system user. Used by systemd-sysusers.
# Deploy with: install -m 644 paceq.sysusers.conf /etc/sysusers.d/paceq.conf
# Then run: systemd-sysusers
u paceq - "paceq orchestrator" /var/lib/paceq /usr/sbin/nologin